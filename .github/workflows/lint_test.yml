
name: lint_test

on:
  - push
  - pull_request

jobs:
  flake8-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: linter-check
        run: |
          make lint
